export enum InviteStatus {
  NONE = 'none',
  PENDING = 'pending',
  INVITED = 'invited',
  FAILED = 'failed',
}

export enum Order {
  ASC = 'asc',
  DESC = 'desc',
}

export enum SportTypeEnum {
  NBA = 'NBA',
  NFL = 'NFL',
  NHL = 'NHL',
  MLB = 'MLB',
}

export enum GameStatusEnum {
  NONE = 'none',
  STARTED = 'started',
  ENDED = 'ended',
}

export enum InviteTypeEnum {
  EMAIL = 'email',
  TEXT = 'text',
}

export enum InviteStatusEnum {
  WAITING = 'waiting',
  COMPLETED = 'completed',
}

export enum ScoringTypeEnum {
  RENEGADES_STANDARD_PPR = 'renegades_standard_ppr',
  HALF_PPR = 'half_ppr',
}

export enum DraftStatusEnum {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
}

export enum LeagueStatusEnum {
  IDLE = 'idle',
  DRAFT = 'draft',
  STARTED = 'started',
  ENDED = 'ended',
}

export enum DraftMessageStatusEnum {
  PENDING = 'pending',
  START = 'start',
  NEXT = 'next',
  COMPLETED = 'completed',
}

export enum PackageTypeEnum {
  SILVER_PACKAGE = 'silver-package',
}

export enum CardTypeEnum {
  CREDIT_CARD = 'credit-card',
}
